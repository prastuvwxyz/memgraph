version: 2

project_name: memgraph

before:
  hooks:
    - go mod tidy

builds:
  - id: memgraph
    main: ./cmd/memgraph
    binary: memgraph
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}
    mod_timestamp: "{{ .CommitTimestamp }}"

universal_binaries:
  - id: memgraph-universal
    ids: [memgraph]
    replace: true
    name_template: memgraph

archives:
  - id: default
    ids: [memgraph, memgraph-universal]
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: prastuvwxyz
    name: memgraph
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
